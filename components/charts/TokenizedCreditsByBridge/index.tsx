import styles from "./styles.module.css";

export default function TokenizedCreditsByBridge(props: {
  children: React.ReactNode;
}) {
  return (
    <div className={styles.chartContainer}>Tokenized credits chart 📈</div>
  );
}
